export function getPalletData(height, weight, lenght, width, quantity, type, overallWeight, overallVolume){
    return {
        height: height,
        weight: weight,
        lenght: lenght,
        width: width,
        quantity: quantity,
        type: type,
        overallWeight: overallWeight,
        overallVolume: overallVolume
    }
}