import { EPAL_TYPE_ID, EPAL_TYPE_VALUE, GENERIC_PALLET_TYPE_ID, GENERIC_PALLET_TYPE_VALUE } from "../constant/costant.js";

export const palletTypeMapper = new Map([
    [GENERIC_PALLET_TYPE_ID, GENERIC_PALLET_TYPE_VALUE],
    [EPAL_TYPE_ID, EPAL_TYPE_VALUE]
]);